const login = document.getElementById('id')
const senha = document.getElementById('senha')
const valida = document.getElementById('acessar')

/*implementar função de validação posteriormente
verificar melhores metodos de validação
if(login || senha == painel_usuarios)....
*/

valida.addEventListener('click', function(){
    console.log('Teste')
});